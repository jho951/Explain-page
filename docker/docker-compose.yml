name: explain-page

services:
  explain-page:
    container_name: explain-page
    image: explain-page:local
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env.local
    ports:
      - "3000:3000"
    restart: unless-stopped
